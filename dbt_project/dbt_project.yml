name: "telemetry_dbt"
version: "1.0.0"

profile: "telemetry_dbt"

model-paths: ["models"]

target-path: "target"
clean-targets:
  - "target"
  - "dbt_packages"

models:
  telemetry_dbt:
    staging:
      materialized: view
    marts:
      materialized: table